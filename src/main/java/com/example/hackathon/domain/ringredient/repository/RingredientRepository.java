package com.example.hackathon.domain.ringredient.repository;

import com.example.hackathon.domain.ringredient.entity.Ringredient;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RingredientRepository extends JpaRepository<Ringredient, Long> {
}
